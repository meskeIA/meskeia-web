# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Archivos generados en build (no necesitan estar en git, Vercel los regenera)
/public/llm-index.json
/public/ai-index.json

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# Playwright MCP screenshots y logs
/.playwright-mcp/
*.playwright.png

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright
/test-results/
/playwright-report/
/playwright/.cache/
.playwright-mcp/

# documentación local (mantener en repo pero ignorar cambios frecuentes)
# docs/ - Comentado para subir la primera vez, luego descomentar si quieres
REORGANIZAR_DOCS.bat
desktop.ini

# scripts de generación de cursos (uso local)
scripts/generate-curso.js
scripts/cursos-generados/

# Documentos estratégicos privados (no subir al repo público).
# Regla: TODO documento interno nuevo nace en _private/ (protección por
# ubicación, no por archivo — así no hay que recordar añadirlo aquí).
_private/

# Credenciales de service accounts (Google Cloud, etc.) - NUNCA subir al repo
.credentials/

# Dumps de la base de datos Turso (backup local, contienen datos de analytics)
_backups/

# Scripts de análisis interno + scratch (consultan analytics privado, no para repo público)
scratch/
scripts/cuadrante-stem.mjs
scripts/detectar-eventos-aula.mjs

# Detector foso-IA (analisis interno)
scripts/detectar-foso-ia.mjs

# Digest diario analytics (analisis interno) + serie histórica generada
scripts/digest-diario.mjs
digests/

# Cruce SEO — medición externa Analytics × GSC × BWT (analisis interno, skill /cruce-seo)
scripts/cruce-seo.mjs
cruce-seo/

# Semilla diaria — generador de ideas (analisis interno, skill /semilla-diaria) + bitácora
scripts/semilla-diaria.mjs
semillas/

